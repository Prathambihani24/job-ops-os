# Database

This directory owns database-related assets.

Current contents:

- `supabase/` for migrations and future policies

Why it exists:

- schema evolution should be isolated from application code
- the repository structure should make the data model easy to review
