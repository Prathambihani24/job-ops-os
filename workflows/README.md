# Workflows

This directory owns workflow-related assets.

Current contents:

- `n8n/` for workflow conventions and future exports

Why it exists:

- workflow logic should be reviewable in Git
- orchestration assets should not be mixed into runtime application code
