# Provider Interfaces

This directory is reserved for provider abstractions and concrete adapters.

Examples:

- `LeadProvider`
- `CompanyResearchProvider`
- `CRMProvider`
- `NotificationProvider`

Optional premium providers should be implemented here without becoming core dependencies.
